import{useState}from"react";import{useProjects}from"../context/ProjectContext";import{formatMoney,projectTotal}from"../utils/calculations";
export default function ProjectManager({onNavigate}){const{state,activeProject,dispatch}=useProjects();const[show,setShow]=useState(false);const[form,setForm]=useState({name:"",code:"",client:"",currency:"TRY"});const submit=e=>{e.preventDefault();if(!form.name.trim())return;dispatch({type:"ADD_PROJECT",payload:{name:form.name.trim(),code:form.code.trim()||`PRJ-${String(state.projects.length+1).padStart(3,"0")}`,client:form.client.trim(),currency:form.currency}});setForm({name:"",code:"",client:"",currency:"TRY"});setShow(false)};return <><div className="toolbar"><button type="button" className="primary" onClick={()=>setShow(true)}>+ Yeni Proje</button></div><section className="project-grid">{state.projects.map(p=><article key={p.id} className={p.id===activeProject?.id?"project-card selected":"project-card"}><b>{p.code}</b><h3>{p.name}</h3><p>{p.client||"İşveren bilgisi girilmedi"}</p><div className="project-stats"><span>{p.boqRows.length} poz</span><strong>{formatMoney(projectTotal(p),p.currency)}</strong></div><button type="button" className="primary full" onClick={()=>{dispatch({type:"SET_ACTIVE_PROJECT",projectId:p.id});onNavigate("boq")}}>Projeyi Aç</button></article>)}</section>{show&&<div className="modal-backdrop" onMouseDown={()=>setShow(false)}><form className="modal" onSubmit={submit} onMouseDown={e=>e.stopPropagation()}><h2>Yeni Proje</h2><label>Proje Adı<input autoFocus value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Proje Kodu<input value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/></label><label>İşveren<input value={form.client} onChange={e=>setForm({...form,client:e.target.value})}/></label><label>Para Birimi<select value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}><option>TRY</option><option>EUR</option><option>USD</option><option>GBP</option></select></label><div className="modal-actions"><button type="button" className="secondary" onClick={()=>setShow(false)}>Vazgeç</button><button type="submit" className="primary">Projeyi Oluştur</button></div></form></div>}</>}
