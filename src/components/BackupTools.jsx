import{useProjects}from"../context/ProjectContext";
export default function BackupTools(){const{state,dispatch}=useProjects();const download=()=>{const b=new Blob([JSON.stringify(state,null,2)],{type:"application/json"});const u=URL.createObjectURL(b);const a=document.createElement("a");a.href=u;a.download=`AS_BOQ_PRO_v4_Yedek_${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(u)};const restore=e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const x=JSON.parse(r.result);if(!x?.projects?.length)throw Error();dispatch({type:"RESTORE_STATE",payload:x});alert("Yedek başarıyla yüklendi.")}catch{alert("Geçersiz yedek dosyası.")}};r.readAsText(f);e.target.value=""};return <div className="backup-tools"><button type="button" className="secondary" onClick={download}>Yedek Al</button><label className="secondary file-label">Yedek Yükle<input type="file" accept=".json" hidden onChange={restore}/></label></div>}
